name: dqn-flappy-bird-cuda
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - matplotlib
  - pyyaml
  - pytorch
  - torchvision
  - pytorch-cuda=12.4
  - gymnasium>=1.0
  - opencv
  - optuna>=3.4          # hyperparameter tuning (src/tune.py)
  - scikit-learn         # optuna param-importance plots
  - pip
  - pip:
    - flappy-bird-gymnasium
    - moviepy
    - grad-cam
